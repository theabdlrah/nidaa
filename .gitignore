node_modules
.next
out
data/db.json
data/verify-audit.json
*.log
npm-debug.log*
.DS_Store

# TS incremental build info
tsconfig.tsbuildinfo

# research scratch dumps
.research/

# python build cache
__pycache__/
*.pyc

# local env / secrets (verifier + admin tokens) — NEVER commit
.env.local
.env*.local
